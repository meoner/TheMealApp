import React from 'react';
import {View, Text, TouchableOpacity, Image} from 'react-native';

function MealDetail({data, onSelect}) {
  return (
    <View>
      <Text>{}</Text>
    </View>
  );
}

export {MealDetail};
